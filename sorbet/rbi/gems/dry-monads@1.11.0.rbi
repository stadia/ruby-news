# typed: true

# DO NOT EDIT MANUALLY
# This is an autogenerated file for types exported from the `dry-monads` gem.
# Please instead update this file by running `bin/tapioca gem dry-monads`.


# pkg:gem/dry-monads#lib/dry/monads/constants.rb:3
module Dry; end

# Common, idiomatic monads for Ruby
#
# @api public
# Common, idiomatic monads for Ruby
#
# @api private
#
# pkg:gem/dry-monads#lib/dry/monads/constants.rb:4
module Dry::Monads
  include ::Dry::Core::Constants
  extend ::Dry::Core::Extensions
  extend ::Dry::Monads::Result::Mixin::Constructors
  extend ::Dry::Monads::Maybe::Mixin::Constructors
  extend ::Dry::Monads::Validated::Mixin::Constructors

  class << self
    # Creates a module that has two methods: `Success` and `Failure`.
    # `Success` is identical to {Result::Mixin::Constructors#Success} and Failure
    # rejects values that don't conform the value of the `error`
    # parameter. This is essentially a Result type with the `Failure` part
    # fixed.
    #
    # @example using dry-types
    #   module Types
    #     include Dry::Types.module
    #   end
    #
    #   class Operation
    #     # :user_not_found and :account_not_found are the only
    #     # values allowed as failure results
    #     Error =
    #       Types.Value(:user_not_found) |
    #       Types.Value(:account_not_found)
    #
    #     include Dry::Monads::Result(Error)
    #
    #     def find_account(id)
    #       account = acount_repo.find(id)
    #
    #       account ? Success(account) : Failure(:account_not_found)
    #     end
    #
    #     def find_user(id)
    #       # ...
    #     end
    #   end
    #
    # @param error [#===] the type of allowed failures
    # @return [Module]
    #
    # pkg:gem/dry-monads#lib/dry/monads/result.rb:363
    def Result(error, **options); end

    # Build a module with cherry-picked monads.
    # It saves a bit of typing when you add multiple
    # monads to one class. Not loaded monads get loaded automatically.
    #
    # @example
    #   require 'dry/monads'
    #
    #   class CreateUser
    #     include Dry::Monads[:result, :do]
    #
    #     def initialize(repo, send_email)
    #       @repo = repo
    #       @send_email = send_email
    #     end
    #
    #     def call(name)
    #       if @repo.user_exist?(name)
    #         Failure(:user_exists)
    #       else
    #         user = yield @repo.add_user(name)
    #         yield @send_email.(user)
    #         Success(user)
    #       end
    #     end
    #   end
    #
    # @param [Array<Symbol>] monads
    # @return [Module]
    # @api public
    #
    # pkg:gem/dry-monads#lib/dry/monads.rb:70
    def [](*monads); end

    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads.rb:33
    def included(base); end

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads.rb:17
    def loader; end

    protected

    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:30
    def registry=(registry); end

    private

    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:66
    def all_loaded?; end

    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:59
    def constructors; end

    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:46
    def known_monads; end

    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:49
    def load_monad(name); end

    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:37
    def register_mixin(name, mod); end

    # pkg:gem/dry-monads#lib/dry/monads/registry.rb:28
    def registry; end
  end
end

# Improper use of None
#
# pkg:gem/dry-monads#lib/dry/monads/errors.rb:23
class Dry::Monads::ConstructorNotAppliedError < ::NoMethodError
  # pkg:gem/dry-monads#lib/dry/monads/errors.rb:24
  def initialize(method_name, constructor_name); end
end

# pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:5
module Dry::Monads::ConversionStubs
  class << self
    # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:6
    def [](*method_names); end
  end
end

# pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:16
module Dry::Monads::ConversionStubs::Methods
  private

  # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:19
  def to_maybe; end

  # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:23
  def to_result; end

  # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:27
  def to_validated; end

  class << self
    # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:19
    def to_maybe; end

    # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:23
    def to_result; end

    # pkg:gem/dry-monads#lib/dry/monads/conversion_stubs.rb:27
    def to_validated; end
  end
end

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/curry.rb:6
module Dry::Monads::Curry
  class << self
    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/curry.rb:8
    def call(value); end
  end
end

# An implementation of do-notation.
#
# @see Do.for
#
# pkg:gem/dry-monads#lib/dry/monads/do.rb:8
module Dry::Monads::Do
  extend ::Dry::Monads::Do::Mixin
  include ::Dry::Monads::Do::All

  class << self
    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do.rb:149
    def coerce_to_monad(monads); end

    # Generates a module that passes a block to methods
    # that either unwraps a single-valued monadic value or halts
    # the execution.
    #
    # @example A complete example
    #
    #   class CreateUser
    #     include Dry::Monads::Result::Mixin
    #     include Dry::Monads::Try::Mixin
    #     include Dry::Monads::Do.for(:call)
    #
    #     attr_reader :user_repo
    #
    #     def initialize(user_repo)
    #       @user_repo = user_repo
    #     end
    #
    #     def call(params)
    #       json = yield parse_json(params)
    #       hash = yield validate(json)
    #
    #       user_repo.transaction do
    #         user = yield create_user(hash[:user])
    #         yield create_profile(user, hash[:profile])
    #       end
    #
    #       Success(user)
    #     end
    #
    #     private
    #
    #     def parse_json(params)
    #       Try(JSON::ParserError) {
    #         JSON.parse(params)
    #       }.to_result
    #     end
    #
    #     def validate(json)
    #       UserSchema.(json).to_monad
    #     end
    #
    #     def create_user(user_data)
    #       Try(Sequel::Error) {
    #         user_repo.create(user_data)
    #       }.to_result
    #     end
    #
    #     def create_profile(user, profile_data)
    #       Try(Sequel::Error) {
    #         user_repo.create_profile(user, profile_data)
    #       }.to_result
    #     end
    #   end
    #
    # @param [Array<Symbol>] methods
    # @return [Module]
    #
    # pkg:gem/dry-monads#lib/dry/monads/do.rb:105
    def for(*methods); end

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do.rb:165
    def halt(result); end

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do.rb:116
    def included(base); end

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do.rb:138
    def method_visibility(mod, method); end

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do.rb:125
    def wrap_method(target, method, visibility); end
  end
end

# Do::All automatically wraps methods defined in a class with an unwrapping block.
# Similar to what `Do.for(...)` does except wraps every method so you don't have
# to list them explicitly.
#
# @example annotated example
#
#   require 'dry/monads/do/all'
#   require 'dry/monads/result'
#
#   class CreateUser
#     include Dry::Monads::Do::All
#     include Dry::Monads::Result::Mixin
#
#     def call(params)
#       # Unwrap a monadic value using an implicitly passed block
#       # if `validates` returns Failure, the execution will be halted
#       values = yield validate(params)
#       user = create_user(values)
#       # If another block is passed to a method then takes
#       # precedence over the unwrapping block
#       safely_subscribe(values[:email]) { Logger.info("Already subscribed") }
#
#       Success(user)
#     end
#
#     def validate(params)
#       if params.key?(:email)
#         Success(email: params[:email])
#       else
#         Failure(:no_email)
#       end
#     end
#
#     def create_user(user)
#       # Here a block is passed to the method but we don't use it
#       UserRepo.new.add(user)
#     end
#
#     def safely_subscribe(email)
#       repo = SubscriptionRepo.new
#
#       if repo.subscribed?(email)
#          # This calls the logger because a block
#          # explicitly passed from `call`
#          yield
#       else
#         repo.subscribe(email)
#       end
#     end
#   end
#
# pkg:gem/dry-monads#lib/dry/monads/do/all.rb:57
module Dry::Monads::Do::All
  extend ::Dry::Monads::Do::All::InstanceMixin

  class << self
    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:102
    def included(base); end

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:113
    def wrap_defined_methods(klass, target); end
  end
end

# @api private
#
# pkg:gem/dry-monads#lib/dry/monads/do/all.rb:129
module Dry::Monads::Do::All::InstanceMixin
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:131
  def extended(object); end
end

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/do/all.rb:59
class Dry::Monads::Do::All::MethodTracker < ::Module
  # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:62
  def initialize(wrappers); end

  # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:89
  def extend_object(target); end

  # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:94
  def wrap_method(target, method); end

  # pkg:gem/dry-monads#lib/dry/monads/do/all.rb:60
  def wrappers; end
end

# @api private
#
# pkg:gem/dry-monads#lib/dry/monads/do.rb:18
class Dry::Monads::Do::Halt < ::StandardError
  # pkg:gem/dry-monads#lib/dry/monads/do.rb:22
  def initialize(result); end

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/do.rb:20
  def result; end
end

# @api private
#
# pkg:gem/dry-monads#lib/dry/monads/do.rb:30
class Dry::Monads::Do::MethodTracker < ::Module
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/do.rb:32
  def initialize(tracked_methods, base, wrapper); end
end

# Do notation as a mixin.
# You can use it in any place in your code, see examples.
#
# @example class-level mixin
#
#   class CreateUser
#     extend Dry::Monads::Do::Mixin
#     extend Dry::Monads[:result]
#
#     def self.run(params)
#       self.call do
#         values = bind Validator.validate(params)
#         user = bind UserRepository.create(values)
#
#         Success(user)
#       end
#     end
#   end
#
# @example using methods defined on Do
#
#   create_user = proc do |params|
#     Do.() do
#       values = bind validate(params)
#       user = bind user_repo.create(values)
#
#       Success(user)
#     end
#   end
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/do/mixin.rb:37
module Dry::Monads::Do::Mixin
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/do/mixin.rb:46
  def bind(monads); end

  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/do/mixin.rb:39
  def call; end
end

# pkg:gem/dry-monads#lib/dry/monads/do.rb:11
Dry::Monads::Do::VISIBILITY_WORD = T.let(T.unsafe(nil), Hash)

# pkg:gem/dry-monads#lib/dry/monads/extensions/pretty_print.rb:5
module Dry::Monads::Extensions; end

# pkg:gem/dry-monads#lib/dry/monads/extensions/pretty_print.rb:6
module Dry::Monads::Extensions::PrettyPrint; end

# pkg:gem/dry-monads#lib/dry/monads/extensions/pretty_print.rb:27
class Dry::Monads::Extensions::PrettyPrint::LazyPrintValue < ::Module
  # pkg:gem/dry-monads#lib/dry/monads/extensions/pretty_print.rb:28
  def initialize(constructor, success_prefix: T.unsafe(nil), error_prefix: T.unsafe(nil)); end
end

# pkg:gem/dry-monads#lib/dry/monads/extensions/pretty_print.rb:7
class Dry::Monads::Extensions::PrettyPrint::PrintValue < ::Module
  # pkg:gem/dry-monads#lib/dry/monads/extensions/pretty_print.rb:8
  def initialize(constructor, accessor: T.unsafe(nil)); end
end

# @see Result::Failure
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:328
Dry::Monads::Failure = Dry::Monads::Result::Failure

# @see Validated::Invalid
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:273
Dry::Monads::Invalid = Dry::Monads::Validated::Invalid

# An error thrown on returning a Failure of unknown type.
#
# pkg:gem/dry-monads#lib/dry/monads/errors.rb:16
class Dry::Monads::InvalidFailureTypeError < ::StandardError
  # pkg:gem/dry-monads#lib/dry/monads/errors.rb:17
  def initialize(failure); end
end

# Lazy is a twin of Task which is always executed on the current thread.
# The underlying mechanism provided by concurrent-ruby ensures the given
# computation is evaluated not more than once (compare with the built-in
# lazy assignement ||= which does not guarantee this).
#
# pkg:gem/dry-monads#lib/dry/monads/lazy.rb:11
class Dry::Monads::Lazy < ::Dry::Monads::Task
  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:44
  def complete?(*args, &block); end

  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:43
  def evaluated?; end

  # Forces the computation. Note that if the computation
  # thrown an error it won't be re-raised as opposed to value!/force!.
  #
  # @return [Lazy]
  #
  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:37
  def force; end

  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:31
  def force!; end

  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:62
  def inspect; end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:49
  def to_s; end

  # Forces the compution and returns its value.
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:30
  def value!; end

  class << self
    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:16
    def new(promise = T.unsafe(nil), &_arg1); end

    private

    # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:24
    def [](executor, &_arg1); end
  end
end

# Lazy constructors
#
# pkg:gem/dry-monads#lib/dry/monads/lazy.rb:66
module Dry::Monads::Lazy::Mixin
  include ::Dry::Monads::Lazy::Mixin::Constructors
end

# Lazy constructors
#
# pkg:gem/dry-monads#lib/dry/monads/lazy.rb:74
module Dry::Monads::Lazy::Mixin::Constructors
  # Lazy computation contructor
  #
  # @param block [Proc]
  # @return [Lazy]
  #
  # pkg:gem/dry-monads#lib/dry/monads/lazy.rb:79
  def Lazy(&_arg0); end
end

# @see Dry::Monads::Lazy
#
# pkg:gem/dry-monads#lib/dry/monads/lazy.rb:68
Dry::Monads::Lazy::Mixin::Lazy = Dry::Monads::Lazy

# @see Dry::Monads::Unit
#
# pkg:gem/dry-monads#lib/dry/monads/lazy.rb:71
Dry::Monads::Lazy::Mixin::Unit = T.let(T.unsafe(nil), Object)

# The List monad.
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:6
class Dry::Monads::List
  include ::Dry::Core::Equalizer::Methods
  include ::Dry::Monads::Transformer
  extend ::Dry::Core::Deprecations::Interface

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:83
  def initialize(value, type = T.unsafe(nil)); end

  # Concatenates two lists.
  #
  # @example
  #   Dry::Monads::List[1, 2] + Dry::Monads::List[3, 4] # => List[1, 2, 3, 4]
  #
  # @param other [List] Other list
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:145
  def +(other); end

  # Applies the stored functions to the elements of the given list.
  #
  # @param list [List]
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:282
  def apply(list = T.unsafe(nil), &_arg1); end

  # Lifts a block/proc and runs it against each member of the list.
  # The block must return a value coercible to a list.
  # As in other monads if no block given the first argument will
  # be treated as callable and used instead.
  #
  # @example
  #   Dry::Monads::List[1, 2].bind { |x| [x + 1] } # => List[2, 3]
  #   Dry::Monads::List[1, 2].bind(-> x { [x, x + 1] }) # => List[1, 2, 2, 3]
  #
  # @param args [Array<Object>] arguments will be passed to the block or proc
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:99
  def bind(*args); end

  # Iterates over the list and collects Some values.
  #
  # @example with block syntax
  #   n = 20
  #   List[10, 5, 0].collect do |divisor|
  #     if divisor.zero?
  #       None()
  #     else
  #       Some(n / divisor)
  #     end
  #   end
  #   # => List[2, 4]
  #
  # @example without block
  #   List[Some(5), None(), Some(3)].collect.map { |x| x * 2 }
  #   # => [10, 6]
  #
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:315
  def collect; end

  # Pattern matching
  #
  # @example
  #   case List[1, 2, 3]
  #   in List[1, 2, x]
  #     x
  #   in List[Integer, _, _]
  #     # ...
  #   in List[0..2, _, _]
  #     # ...
  #   end
  #
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:343
  def deconstruct; end

  # Whether the list is empty.
  #
  # @return [TrueClass, FalseClass]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:193
  def empty?; end

  # Filters elements with a block
  #
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:203
  def filter(&_arg0); end

  # Returns the first element.
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:166
  def first; end

  # Maps a block over the list. Acts as `Array#map`.
  # As in other monads if no block given the first argument will
  # be treated as callable and used instead.
  #
  # @example
  #   Dry::Monads::List[1, 2].fmap { |x| x + 1 } # => List[2, 3]
  #
  # @param args [Array<Object>] arguments will be passed to the block or proc
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:117
  def fmap(*args); end

  # Folds the list from the left.
  #
  # @param initial [Object] Initial value
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:177
  def fold_left(initial, &_arg1); end

  # Folds the list from the right.
  #
  # @param initial [Object] Initial value
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:185
  def fold_right(initial); end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:178
  def foldl(initial, &_arg1); end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:188
  def foldr(initial); end

  # Returns the first element wrapped with a `Maybe`.
  #
  # @return [Maybe<Object>]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:219
  def head; end

  # Returns a string representation of the list.
  #
  # @example
  #   Dry::Monads::List[1, 2, 3].inspect # => "List[1, 2, 3]"
  #
  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:153
  def inspect; end

  # Returns the last element.
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:171
  def last; end

  # Maps a block over the list. Acts as `Array#map`.
  # If called without a block, this method returns an enumerator, not a List
  #
  # @return [List,Enumerator]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:130
  def map(&block); end

  # Returns the List monad.
  #
  # @return [Monad]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:290
  def monad; end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:179
  def reduce(initial, &_arg1); end

  # Reverses the list.
  #
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:214
  def reverse; end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:204
  def select(&_arg0); end

  # List size.
  #
  # @return [Integer]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:209
  def size; end

  # Sorts the list.
  #
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:198
  def sort; end

  # Returns list's tail.
  #
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:224
  def tail; end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:161
  def to_a; end

  # Coerces to an array
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:160
  def to_ary; end

  # Returns self.
  #
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:295
  def to_monad; end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:157
  def to_s; end

  # Traverses the list with a block (or without it).
  # This methods "flips" List structure with the given monad (obtained from the type).
  # Note that traversing requires the list to be typed.
  # Also if a block given, its returning type must be equal list's type.
  #
  # @example
  #   List<Result>[Success(1), Success(2)].traverse # => Success([1, 2])
  #   List<Maybe>[Some(1), None, Some(3)].traverse # => None
  #
  # @return [Monad] Result is a monadic value
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:263
  def traverse(proc = T.unsafe(nil), &block); end

  # Internal array value
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:80
  def type; end

  # Turns the list into a typed one.
  # Type is required for some operations like .traverse.
  #
  # @param type [Monad] Monad instance
  # @return [List] Typed list
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:231
  def typed(type = T.unsafe(nil)); end

  # Whether the list is types
  #
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:251
  def typed?; end

  # Internal array value
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:80
  def value; end

  private

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:347
  def coerce(other); end

  class << self
    # Builds a list.
    #
    # @param values [Array<Object>] List elements
    # @return [List]
    #
    # pkg:gem/dry-monads#lib/dry/monads/list.rb:12
    def [](*values); end

    # Coerces a value to a list. `nil` will be coerced to an empty list.
    #
    # @param value [Object] Value
    # @param type [Monad] Embedded monad type (used in case of list of monadic values)
    # @return [List]
    #
    # pkg:gem/dry-monads#lib/dry/monads/list.rb:19
    def coerce(value, type = T.unsafe(nil)); end

    # Wraps a value with a list.
    #
    # @param value [Object] any object
    # @return [List]
    #
    # pkg:gem/dry-monads#lib/dry/monads/list.rb:39
    def pure(value = T.unsafe(nil), type = T.unsafe(nil), &block); end

    # Iteratively builds a new list from a block returning Maybe values
    #
    # @see https://hackage.haskell.org/package/base-4.12.0.0/docs/Data-List.html#g:9
    #
    # @param state [Object.new] Initial state
    # @param type [#pure] Type of list element
    # @return [List]
    #
    # pkg:gem/dry-monads#lib/dry/monads/list.rb:56
    def unfold(state, type = T.unsafe(nil)); end
  end
end

# Empty list
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:350
Dry::Monads::List::EMPTY = T.let(T.unsafe(nil), Dry::Monads::List)

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:353
class Dry::Monads::List::ListBuilder
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:360
  def initialize(type); end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:362
  def [](*args); end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:364
  def coerce(value); end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:366
  def pure(val = T.unsafe(nil), &block); end

  # pkg:gem/dry-monads#lib/dry/monads/list.rb:358
  def type; end

  class << self
    # pkg:gem/dry-monads#lib/dry/monads/list.rb:355
    def [](*_arg0); end
  end
end

# List of maybes
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:379
Dry::Monads::List::Maybe = T.let(T.unsafe(nil), Dry::Monads::List::ListBuilder)

# List contructors.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:390
module Dry::Monads::List::Mixin
  # List constructor.
  # @return [List]
  #
  # pkg:gem/dry-monads#lib/dry/monads/list.rb:399
  def List(value); end
end

# @see Dry::Monads::List
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:395
Dry::Monads::List::Mixin::L = Dry::Monads::List

# @see Dry::Monads::List
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:392
Dry::Monads::List::Mixin::List = Dry::Monads::List

# List of results
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:376
Dry::Monads::List::Result = T.let(T.unsafe(nil), Dry::Monads::List::ListBuilder)

# List of tasks
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:373
Dry::Monads::List::Task = T.let(T.unsafe(nil), Dry::Monads::List::ListBuilder)

# List of tries
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:382
Dry::Monads::List::Try = T.let(T.unsafe(nil), Dry::Monads::List::ListBuilder)

# List of validation results
#
# pkg:gem/dry-monads#lib/dry/monads/list.rb:385
Dry::Monads::List::Validated = T.let(T.unsafe(nil), Dry::Monads::List::ListBuilder)

# Represents a value which can exist or not, i.e. it could be nil.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:8
class Dry::Monads::Maybe
  include ::Dry::Monads::Transformer
  extend ::Dry::Core::Constants
  extend ::Dry::Core::ClassAttributes

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:50
  def failure?; end

  # Returns the Maybe monad.
  # This is how we're doing polymorphism in Ruby 😕
  #
  # @return [Monad]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:70
  def monad; end

  # Returns true for an instance of a {Maybe::None} monad.
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:49
  def none?; end

  # Returns true for an instance of a {Maybe::Some} monad.
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:53
  def some?; end

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:54
  def success?; end

  # Returns self, added to keep the interface compatible with that of Either monad types.
  #
  # @return [Maybe::Some, Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:59
  def to_maybe; end

  # Returns self.
  #
  # @return [Maybe::Some, Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:64
  def to_monad; end

  class << self
    # Wraps the given value with into a Maybe object.
    #
    # @param value [Object] value to be stored in the monad
    # @return [Maybe::Some, Maybe::None]
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:22
    def coerce(value); end

    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:29
    def lift(*args, &block); end

    # Wraps the given value with `Some`.
    #
    # @param value [Object] value to be wrapped with Some
    # @param block [Object] block to be wrapped with Some
    # @return [Maybe::Some]
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:36
    def pure(value = T.unsafe(nil), &block); end

    # Reutrns a Some wrapper converted to a block
    #
    # @return [Proc]
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:43
    def to_proc; end
  end
end

# Utilities for working with hashes storing Maybe values
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:308
module Dry::Monads::Maybe::Hash
  class << self
    # Traverses a hash with maybe values. If any value is None then None is returned
    #
    # @example
    #   Maybe::Hash.all(foo: Some(1), bar: Some(2)) # => Some(foo: 1, bar: 2)
    #   Maybe::Hash.all(foo: Some(1), bar: None())  # => None()
    #   Maybe::Hash.all(foo: None(), bar: Some(2))  # => None()
    #
    # @param hash [::Hash<Object,Maybe>]
    # @return [Maybe<::Hash>]
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:319
    def all(hash, trace = T.unsafe(nil)); end

    # Traverses a hash with maybe values. Some values are unwrapped, keys with
    # None values are removed
    #
    # @example
    #   Maybe::Hash.filter(foo: Some(1), bar: Some(2)) # => { foo: 1, bar: 2 }
    #   Maybe::Hash.filter(foo: Some(1), bar: None())  # => { foo: 1 }
    #   Maybe::Hash.filter(foo: None(), bar: Some(2))  # => { bar: 2 }
    #
    # @param hash [::Hash<Object,Maybe>]
    # @return [::Hash]
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:342
    def filter(hash); end
  end
end

# A module that can be included for easier access to Maybe monads.
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:271
module Dry::Monads::Maybe::Mixin
  include ::Dry::Monads::Maybe::Mixin::Constructors
end

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:280
module Dry::Monads::Maybe::Mixin::Constructors
  # @param value [Object] the value to be stored in the monad
  # @return [Maybe::Some, Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:283
  def Maybe(value); end

  # @return [Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:301
  def None; end

  # Some constructor
  #
  # @overload Some(value)
  #   @param value [Object] any value except `nil`
  #   @return [Maybe::Some]
  #
  # @overload Some(&block)
  #   @param block [Proc] a block to be wrapped with Some
  #   @return [Maybe::Some]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:295
  def Some(value = T.unsafe(nil), &block); end
end

# @see Dry::Monads::Maybe
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:273
Dry::Monads::Maybe::Mixin::Maybe = Dry::Monads::Maybe

# @see Maybe::None
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:277
Dry::Monads::Maybe::Mixin::None = Dry::Monads::Maybe::None

# @see Maybe::Some
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:275
Dry::Monads::Maybe::Mixin::Some = Dry::Monads::Maybe::Some

# Represents an absence of a value, i.e. the value nil.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:177
class Dry::Monads::Maybe::None < ::Dry::Monads::Maybe
  include ::Dry::Monads::RightBiased::Left

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:198
  def initialize(trace = T.unsafe(nil)); end

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:244
  def ==(other); end

  # Pattern matching
  #
  # @example
  #   case Some(:foo)
  #   in Some(Integer)
  #     # ...
  #   in Some(:bar)
  #     # ...
  #   in None()
  #     # ...
  #   end
  #
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:262
  def deconstruct; end

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:242
  def eql?(other); end

  # @see Maybe::Some#filter
  #
  # @return [Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:267
  def filter(_ = T.unsafe(nil), &_arg1); end

  # @private
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:247
  def hash; end

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:239
  def inspect; end

  # @!method maybe(*args, &block)
  #   Alias of fmap, returns self back
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:205
  def maybe(*_arg0, **_arg1, &_arg2); end

  # If a block is given passes internal value to it and returns the result,
  # otherwise simply returns the parameter val.
  #
  # @example
  #   Dry::Monads.None.or('no value') # => "no value"
  #   Dry::Monads.None.or { Time.now } # => current time
  #
  # @param args [Array<Object>] if no block given the first argument will be returned
  #                             otherwise arguments will be transparently passed to the block
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:217
  def or(*args); end

  # A lifted version of `#or`. Applies `Maybe.coerce` to the passed value or
  # to the block result.
  #
  # @example
  #   Dry::Monads.None.or_fmap('no value') # => Some("no value")
  #   Dry::Monads.None.or_fmap { Time.now } # => Some(current time)
  #
  # @param args [Array<Object>] arguments will be passed to the underlying `#or` call
  # @return [Maybe::Some, Maybe::None] Lifted `#or` result, i.e. nil will be mapped to None,
  #                                    other values will be wrapped with Some
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:235
  def or_fmap(*_arg0, **_arg1, &_arg2); end

  # Converts to Failure(fallback_value)
  #
  # @param fail [#call] Fallback value
  # @param block [Proc] Fallback block
  # @return [Failure<Any>]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:383
  def to_result(fail = T.unsafe(nil)); end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:238
  def to_s; end

  # Line where the value was constructed
  #
  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:196
  def trace; end

  class << self
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:181
    def instance; end

    private

    # @api private
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:184
    def method_missing(m, *_arg1); end
  end
end

# Represents a value that is present, i.e. not nil.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:75
class Dry::Monads::Maybe::Some < ::Dry::Monads::Maybe
  include ::Dry::Core::Equalizer::Methods
  include ::Dry::Monads::RightBiased::Right

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:91
  def initialize(value = T.unsafe(nil)); end

  # Accepts a block and runs it against the wrapped value.
  # If the block returns a truthy value the result is self,
  # otherwise None. If no block is given, the value serves
  # as its result.
  #
  # @param with [#call] positional block
  # @param block [Proc] block
  #
  # @return [Maybe::None, Maybe::Some]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:153
  def filter(with = T.unsafe(nil), &block); end

  # Does the same thing as #bind except it also wraps the value
  # in an instance of Maybe::Some monad. This allows for easier
  # chaining of calls.
  #
  # @example
  #   Dry::Monads.Some(4).fmap(&:succ).fmap(->(n) { n**2 }) # => Some(25)
  #
  # @param args [Array<Object>] arguments will be transparently passed through to #bind
  # @return [Maybe::Some, Maybe::None] Wrapped result, i.e. nil will be mapped to None,
  #                                    other values will be wrapped with Some
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:109
  def fmap(*_arg0, **_arg1, &_arg2); end

  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:171
  def inspect; end

  # Does the same thing as #bind except it also wraps the value
  # in an instance of the Maybe monad. This allows for easier
  # chaining of calls.
  #
  # @example
  #   Dry::Monads.Some(4).maybe(&:succ).maybe(->(n) { n**2 }) # => Some(25)
  #   Dry::Monads.Some(4).maybe(&:succ).maybe(->(_) { nil }) # => None()
  #
  # @param args [Array<Object>] arguments will be transparently passed through to #bind
  # @return [Maybe::Some, Maybe::None] Wrapped result, i.e. nil will be mapped to None,
  #                                    other values will be wrapped with Some
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:142
  def maybe(*_arg0, **_arg1, &_arg2); end

  # Converts to Sucess(value!)
  #
  # @param _fail [#call] Fallback value
  # @param block [Proc] Fallback block
  # @return [Success<Any>]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:374
  def to_result(_fail = T.unsafe(nil), &_arg1); end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:164
  def to_s; end

  class << self
    # Shortcut for Some([...])
    #
    #  @example
    #    include Dry::Monads[:maybe]
    #
    #    def call
    #      Some[200, {}, ['ok']] # => Some([200, {}, ['ok']])
    #    end
    #
    # @api public
    #
    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:89
    def [](*value); end

    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:77
    def call(*_arg0); end

    # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:77
    def to_proc; end
  end
end

# @see Maybe::None
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:355
Dry::Monads::None = Dry::Monads::Maybe::None

# Represents an operation which either succeeded or failed.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:8
class Dry::Monads::Result
  include ::Dry::Monads::Transformer

  # @return [Object] Error
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:16
  def failure; end

  # Returns the Result monad.
  # This is how we're doing polymorphism in Ruby 😕
  #
  # @return [Monad]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:48
  def monad; end

  # @return [Object] Successful result
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:13
  def success; end

  # Returns self.
  #
  # @return [Result::Success, Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:42
  def to_monad; end

  # Returns self, added to keep the interface compatible with other monads.
  #
  # @return [Result::Success, Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:37
  def to_result; end

  class << self
    # Wraps the given value with Success.
    #
    # @overload pure(value)
    #   @param value [Object]
    #   @return [Result::Success]
    #
    # @overload pure(&block)
    #   @param block [Proc] a block to be wrapped with Success
    #   @return [Result::Success]
    #
    # pkg:gem/dry-monads#lib/dry/monads/result.rb:29
    def pure(value = T.unsafe(nil), &block); end
  end
end

# Represents a value of a failed operation.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:135
class Dry::Monads::Result::Failure < ::Dry::Monads::Result
  include ::Dry::Monads::RightBiased::Left
  include ::Dry::Core::Equalizer::Methods

  # @param value [Object] failure value
  # @param trace [String] caller line
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:170
  def initialize(value, trace = T.unsafe(nil)); end

  # @param other [Result]
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:246
  def ===(other); end

  # Lifts a block/proc over Failure
  #
  # @overload alt_map(proc)
  #   @param proc [#call]
  #   @return [Result::Failure]
  #
  # @overload alt_map
  #   @param block [Proc]
  #   @return [Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:270
  def alt_map(proc = T.unsafe(nil), &block); end

  # Returns result of applying second function to the internal value.
  #
  # @example
  #   Dry::Monads.Failure(1).either(-> x { x + 1 }, -> x { x + 2 }) # => 3
  #
  # @param _ [#call] Ignored
  # @param g [#call] Function to call
  # @return [Any] Return value of `g`
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:258
  def either(_, g); end

  # @private
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:177
  def failure; end

  # Returns true
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:185
  def failure?; end

  # Transform to a Success instance
  #
  # @return [Result::Success]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:233
  def flip; end

  # pkg:gem/dry-monads#lib/dry/monads/result.rb:228
  def inspect; end

  # If a block is given passes internal value to it and returns the result,
  # otherwise simply returns the first argument.
  #
  # @example
  #   Dry::Monads.Failure(ArgumentError.new('error message')).or(&:message)
  #   # => "error message"
  #
  # @param args [Array<Object>] arguments that will be passed to a block
  #                             if one was given, otherwise the first
  #                             value will be returned
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:201
  def or(*args); end

  # A lifted version of `#or`. Wraps the passed value or the block
  # result with Result::Success.
  #
  # @example
  #   Dry::Monads.Failure.new('no value').or_fmap('value') # => Success("value")
  #   Dry::Monads.Failure.new('no value').or_fmap { 'value' } # => Success("value")
  #
  # @param args [Array<Object>] arguments will be passed to the underlying `#or` call
  # @return [Result::Success] Wrapped value
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:218
  def or_fmap(*_arg0, **_arg1, &_arg2); end

  # Apply the first function to value.
  #
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:182
  def result(f, _); end

  # Returns false
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:188
  def success?; end

  # @return [Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:370
  def to_maybe; end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:221
  def to_s; end

  # Transforms to Validated
  #
  # @return [Validated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:287
  def to_validated; end

  # Line where the value was constructed
  #
  # @return [String]
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:166
  def trace; end

  # @see RightBiased::Left#value_or
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:236
  def value_or(val = T.unsafe(nil)); end

  class << self
    # Shortcut for Failure([...])
    #
    #  @example
    #    include Dry::Monads[:result]
    #
    #    def call
    #      Failure[:error, :not_found] # => Failure([:error, :not_found])
    #    end
    #
    # @api public
    #
    # pkg:gem/dry-monads#lib/dry/monads/result.rb:151
    def [](*value); end

    # pkg:gem/dry-monads#lib/dry/monads/result.rb:139
    def call(*_arg0); end

    # Returns a constructor proc
    #
    # @return [Proc]
    #
    # pkg:gem/dry-monads#lib/dry/monads/result.rb:158
    def to_proc; end
  end
end

# @see Monads#Result
# @private
#
# pkg:gem/dry-monads#lib/dry/monads/result/fixed.rb:10
class Dry::Monads::Result::Fixed < ::Module
  # pkg:gem/dry-monads#lib/dry/monads/result/fixed.rb:13
  def initialize(error, **_arg1); end

  private

  # pkg:gem/dry-monads#lib/dry/monads/result/fixed.rb:35
  def included(base); end

  class << self
    # pkg:gem/dry-monads#lib/dry/monads/result/fixed.rb:11
    def [](error, **options); end
  end
end

# A module that can be included for easier access to Result monads.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:279
module Dry::Monads::Result::Mixin
  include ::Dry::Monads::Result::Mixin::Constructors
end

# Value constructors
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:287
module Dry::Monads::Result::Mixin::Constructors
  # Failure constructor
  #
  # @overload Success(value)
  #   @param value [Object]
  #   @return [Result::Failure]
  #
  # @overload Success(&block)
  #   @param block [Proc] a block to be wrapped with Failure
  #   @return [Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:313
  def Failure(value = T.unsafe(nil), &block); end

  # Success constructor
  #
  # @overload Success(value)
  #   @param value [Object]
  #   @return [Result::Success]
  #
  # @overload Success(&block)
  #   @param block [Proc] a block to be wrapped with Success
  #   @return [Result::Success]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:298
  def Success(value = T.unsafe(nil), &block); end
end

# @see Result::Failure
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:283
Dry::Monads::Result::Mixin::Failure = Dry::Monads::Result::Failure

# @see Result::Success
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:281
Dry::Monads::Result::Mixin::Success = Dry::Monads::Result::Success

# Represents a value of a successful operation.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:53
class Dry::Monads::Result::Success < ::Dry::Monads::Result
  include ::Dry::Monads::RightBiased::Right
  include ::Dry::Core::Equalizer::Methods
  extend ::Dry::Core::Deprecations::Interface

  # @param value [Object] a value of a successful operation
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:72
  def initialize(value); end

  # Ignores values and returns self, see {Failure#alt_map}
  #
  # @return [Result::Success]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:129
  def alt_map(_ = T.unsafe(nil), &_arg1); end

  # Returns result of applying first function to the internal value.
  #
  # @example
  #   Dry::Monads.Success(1).either(-> x { x + 1 }, -> x { x + 2 }) # => 2
  #
  # @param f [#call] Function to apply
  # @param _ [#call] Ignored
  # @return [Any] Return value of `f`
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:107
  def either(f, _); end

  # Returns false
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:83
  def failure?; end

  # Transforms to a Failure instance
  #
  # @return [Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:122
  def flip; end

  # Does the same thing as #bind except it also wraps the value
  # in an instance of Result::Success monad. This allows for easier
  # chaining of calls.
  #
  # @example
  #   Dry::Monads.Success(4).fmap(&:succ).fmap(->(n) { n**2 }) # => Success(25)
  #
  # @param args [Array<Object>] arguments will be transparently passed through to #bind
  # @return [Result::Success]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:97
  def fmap(*_arg0, **_arg1, &_arg2); end

  # pkg:gem/dry-monads#lib/dry/monads/result.rb:117
  def inspect; end

  # Apply the second function to value.
  #
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:80
  def result(_, f); end

  # pkg:gem/dry-monads#lib/dry/monads/result.rb:69
  def success; end

  # Returns true
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:86
  def success?; end

  # @return [Maybe]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:362
  def to_maybe; end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:110
  def to_s; end

  # Transforms to Validated
  #
  # @return [Validated::Valid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:280
  def to_validated; end

  class << self
    # Shortcut for Success([...])
    #
    #  @example
    #    include Dry::Monads[:result]
    #
    #    def call
    #      Success[200, {}, ['ok']] # => Success([200, {}, ['ok']])
    #    end
    #
    # @api public
    #
    # pkg:gem/dry-monads#lib/dry/monads/result.rb:67
    def [](*value); end

    # pkg:gem/dry-monads#lib/dry/monads/result.rb:54
    def call(*_arg0); end

    # pkg:gem/dry-monads#lib/dry/monads/result.rb:54
    def to_proc; end
  end
end

# A common module for right-biased monads, such as Result/Either, Maybe, and Try.
#
# pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:6
module Dry::Monads::RightBiased; end

# Left/wrong/erroneous part
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:237
module Dry::Monads::RightBiased::Left
  # Returns self back. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:322
  def and(_, &_arg1); end

  # Ignores the input parameter and returns self. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:304
  def apply(*_arg0, **_arg1, &_arg2); end

  # Ignores the input parameter and returns self. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:249
  def bind(*_arg0, **_arg1, &_arg2); end

  # Pattern matching
  #
  # @example
  #   case Success(x)
  #   in Success(Integer)
  #     # ...
  #   in Success(2..100)
  #     # ...
  #   in Success(2..200 => code)
  #     # ...
  #   in Failure(_)
  #     # ...
  #   end
  #
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:339
  def deconstruct; end

  # Pattern matching hash values
  #
  # @example
  #   case Failure(x)
  #   in Failure(code: 400...500) then :user_error
  #   in Failure(code: 500...600) then :server_error
  #   end
  #
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:358
  def deconstruct_keys(keys); end

  # Returns self back. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:310
  def discard; end

  # Returns self back. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:316
  def flatten; end

  # Ignores the input parameter and returns self. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:261
  def fmap(*_arg0, **_arg1, &_arg2); end

  # Left-biased #bind version.
  #
  # @example
  #   Dry::Monads.Left(ArgumentError.new('error message')).or(&:message) # => "error message"
  #   Dry::Monads.None.or('no value') # => "no value"
  #   Dry::Monads.None.or { Time.now } # => current time
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:271
  def or(*_arg0, **_arg1, &_arg2); end

  # A lifted version of `#or`. This is basically `#or` + `#fmap`.
  #
  # @example
  #   Dry::Monads.None.or_fmap('no value') # => Some("no value")
  #   Dry::Monads.None.or_fmap { Time.now } # => Some(current time)
  #
  # @return [RightBiased::Left, RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:287
  def or_fmap(*_arg0, **_arg1, &_arg2); end

  # Ignores the input parameter and returns self. It exists to keep the interface
  # identical to that of {RightBiased::Right}.
  #
  # @return [RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:255
  def tee(*_arg0, **_arg1, &_arg2); end

  # Raises an error on accessing internal value
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:243
  def value!; end

  # Returns the passed value
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:292
  def value_or(val = T.unsafe(nil)); end

  # Returns the passed value. Works in pair with {RightBiased::Right#|}.
  #
  # @param alt [RightBiased::Right, RightBiased::Left]
  #
  # @return [RightBiased::Right, RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:278
  def |(alt); end

  class << self
    # @private
    # @return [String] Caller location
    #
    # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:240
    def trace_caller; end
  end
end

# Right part
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:10
module Dry::Monads::RightBiased::Right
  # @param other [Object]
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:122
  def ===(other); end

  # Combines the wrapped value with another monadic value.
  # If both values are right-sided, yields a block and passes a tuple
  # of values there. If no block given, returns a tuple of values wrapped with
  # a monadic structure.
  #
  # @example
  #   include Dry::Monads::Result::Mixin
  #
  #   Success(3).and(Success(5)) # => Success([3, 5])
  #   Success(3).and(Failure(:not_a_number)) # => Failure(:not_a_number)
  #   Failure(:not_a_number).and(Success(5)) # => Failure(:not_a_number)
  #   Success(3).and(Success(5)) { |a, b| a + b } # => Success(8)
  #
  # @param mb [RightBiased::Left,RightBiased::Right]
  #
  # @return [RightBiased::Left,RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:163
  def and(mb); end

  # Applies the stored value to the given argument if the argument has type of Right,
  # otherwise returns the argument.
  #
  # @example happy path
  #   create_user = Dry::Monads::Success(CreateUser.new)
  #   name = Success("John")
  #   create_user.apply(name) # equivalent to CreateUser.new.call("John")
  #
  # @example unhappy path
  #   name = Failure(:name_missing)
  #   create_user.apply(name) # => Failure(:name_missing)
  #
  # @return [RightBiased::Left,RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:112
  def apply(val = T.unsafe(nil), &block); end

  # Calls the passed in Proc object with value stored in self
  # and returns the result.
  #
  # If proc is nil, it expects a block to be given and will yield to it.
  #
  # @example
  #   Dry::Monads.Right(4).bind(&:succ) # => 5
  #
  # @param [Array<Object>] args arguments that will be passed to a block
  #                             if one was given, otherwise the first
  #                             value assumed to be a Proc (callable)
  #                             object and the rest of args will be passed
  #                             to this object along with the internal value
  # @return [Object] result of calling proc or block on the internal value
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:40
  def bind(*args, **kwargs); end

  # Pattern matching
  #
  # @example
  #   case Success(x)
  #   in Success(Integer)
  #     # ...
  #   in Success(2..100)
  #     # ...
  #   in Success(2..200 => code)
  #     # ...
  #   end
  #
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:188
  def deconstruct; end

  # Pattern matching hash values
  #
  # @example
  #   case Success(x)
  #   in Success(code: 200...300) then :ok
  #   in Success(code: 300...400) then :redirect
  #   in Success(code: 400...500) then :user_error
  #   in Success(code: 500...600) then :server_error
  #   end
  #
  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:209
  def deconstruct_keys(keys); end

  # Maps the value to Dry::Monads::Unit, useful when you don't care
  # about the actual value.
  #
  # @example
  #   Dry::Monads::Success(:success).discard
  #   # => Success(Unit)
  #
  # @return [RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:134
  def discard; end

  # Removes one level of monad structure by joining two values.
  #
  # @example
  #   include Dry::Monads::Result::Mixin
  #   Success(Success(5)).flatten # => Success(5)
  #   Success(Failure(:not_a_number)).flatten # => Failure(:not_a_number)
  #   Failure(:not_a_number).flatten # => Failure(:not_a_number)
  #
  # @return [RightBiased::Right,RightBiased::Left]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:145
  def flatten; end

  # Abstract method for lifting a block over the monad type.
  # Must be implemented for a right-biased monad.
  #
  # @return [RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:72
  def fmap(*_arg0, **_arg1, &_arg2); end

  # Ignores arguments and returns self. It exists to keep the interface
  # identical to that of {RightBiased::Left}.
  #
  # @return [RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:78
  def or(*_arg0, **_arg1, &_arg2); end

  # A lifted version of `#or`. For {RightBiased::Right} acts in the same way as `#or`,
  # that is returns itselt.
  #
  # @return [RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:92
  def or_fmap(*_arg0, **_arg1, &_arg2); end

  # Does the same thing as #bind except it returns the original monad
  # when the result is a Right.
  #
  # @example
  #   Dry::Monads.Right(4).tee { Right('ok') } # => Right(4)
  #   Dry::Monads.Right(4).tee { Left('fail') } # => Left('fail')
  #
  # @param [Array<Object>] args arguments will be transparently passed through to #bind
  # @return [RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:66
  def tee(*_arg0, **_arg1, &_arg2); end

  # Unwraps the underlying value
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:24
  def value!; end

  # Returns value. It exists to keep the interface identical to that of RightBiased::Left
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:97
  def value_or(_val = T.unsafe(nil), &_arg1); end

  # Ignores arguments and returns self. It exists to keep the interface
  # identical to that of {RightBiased::Left}.
  #
  # @param _alt [RightBiased::Right, RightBiased::Left]
  #
  # @return [RightBiased::Right]
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:86
  def |(_alt); end

  private

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:229
  def curry; end

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:220
  def destructure(value); end

  class << self
    # @private
    #
    # pkg:gem/dry-monads#lib/dry/monads/right_biased.rb:12
    def included(m); end
  end
end

# @see Maybe::Some
#
# pkg:gem/dry-monads#lib/dry/monads/maybe.rb:353
Dry::Monads::Some = Dry::Monads::Maybe::Some

# @see Result::Success
#
# pkg:gem/dry-monads#lib/dry/monads/result.rb:326
Dry::Monads::Success = Dry::Monads::Result::Success

# The Task monad represents an async computation. The implementation
# is a rather thin wrapper of Concurrent::Promise from the concurrent-ruby.
# The API supports setting a custom executor from concurrent-ruby.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/task.rb:12
class Dry::Monads::Task
  extend ::Dry::Core::Deprecations::Interface

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:87
  def initialize(promise); end

  # Compares two tasks. Note, it works
  # good enough only for complete tasks.
  #
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:191
  def ==(other); end

  # Applies the stored value to the given argument.
  #
  # @example
  #   Task.
  #     pure { |x, y| x ** y }.
  #     apply(Task { 2 }).
  #     apply(Task { 3 }).
  #     to_maybe # => Some(8)
  #
  # @param val [Task]
  # @return [Task]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:222
  def apply(val = T.unsafe(nil), &_arg1); end

  # Composes two tasks to run one after another.
  # A more common name is `then` exists as an alias.
  #
  # @param block [Proc] A block that yields the result of the current task
  #                     and returns another task
  # @return [Task]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:119
  def bind(&block); end

  # Whether the computation is complete.
  #
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:201
  def complete?; end

  # Maps a successful result to Unit, effectively discards it
  #
  # @return [Task]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:230
  def discard; end

  # Lifts a block over the Task monad.
  #
  # @param block [Proc]
  # @return [Task]
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:111
  def fmap(&_arg0); end

  # pkg:gem/dry-monads#lib/dry/monads/task.rb:141
  def inspect; end

  # @return [Class]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:204
  def monad; end

  # Rescues the error with a block that returns another task.
  #
  # @param block [Proc]
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:153
  def or(&block); end

  # Transforms the error if the computation wasn't successful.
  #
  # @param block [Proc]
  # @return [Task]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:147
  def or_fmap(&_arg0); end

  # pkg:gem/dry-monads#lib/dry/monads/task.rb:122
  def then(*args, &block); end

  # Converts to Maybe. Blocks the current thread if required.
  #
  # @return [Maybe]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:378
  def to_maybe; end

  # Returns self.
  #
  # @return [Maybe::Some, Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:209
  def to_monad; end

  # Converts to Result. Blocks the current thread if required.
  #
  # @return [Result]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:397
  def to_result; end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:125
  def to_s; end

  # Retrieves the value of the computation.
  # Blocks current thread if the underlying promise
  # hasn't been complete yet.
  # Throws an error if the computation failed.
  #
  # @return [Object]
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:98
  def value!; end

  # Extracts the resulting value if the computation was successful
  # otherwise yields the block and returns its result.
  #
  # @param block [Proc]
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:177
  def value_or(&_arg0); end

  # Blocks the current thread until the task is complete.
  #
  # @return [Task]
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:182
  def wait(timeout = T.unsafe(nil)); end

  protected

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:83
  def promise; end

  private

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:249
  def compare_promises(x, y); end

  # @api private
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:235
  def curry(value); end

  class << self
    # Creates a Task with the given executor
    #
    # @example providing an executor instance, using Ruby 2.5+ syntax
    #   IO = Concurrent::ThreadPoolExecutor.new
    #   Task[IO] { do_http_request }
    #
    # @example using a predefined executor
    #    Task[:fast] { do_quick_task }
    #
    # @param executor [Concurrent::AbstractExecutorService,Symbol]
    #   Either an executor instance
    #   or a name of predefined global
    #   from concurrent-ruby
    #
    # @return [Task]
    #
    # pkg:gem/dry-monads#lib/dry/monads/task.rb:53
    def [](executor, &_arg1); end

    # Returns a failed task from the given exception
    #
    # @param exc [Exception]
    # @return [Task]
    #
    # pkg:gem/dry-monads#lib/dry/monads/task.rb:76
    def failed(exc); end

    # Creates a Task from a block
    #
    # @overload new(promise)
    #   @param promise [Promise]
    #   @return [Task]
    #
    # @overload new(&block)
    #   @param block [Proc] a task to run
    #   @return [Task]
    #
    # pkg:gem/dry-monads#lib/dry/monads/task.rb:30
    def new(promise = T.unsafe(nil), &_arg1); end

    # Returns a completed task from the given value
    #
    # @overload pure(value)
    #   @param value [Object]
    #   @return [Task]
    #
    # @overload pure(&block)
    #   @param block [Proc]
    #   @return [Task]
    #
    # pkg:gem/dry-monads#lib/dry/monads/task.rb:67
    def pure(value = T.unsafe(nil), &block); end
  end
end

# Task constructors.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/task.rb:258
module Dry::Monads::Task::Mixin
  include ::Dry::Monads::Task::Mixin::Constructors

  class << self
    # Created a mixin with the given executor injected.
    #
    # @param executor [Concurrent::AbstractExecutorService,Symbol]
    # @return [Module]
    #
    # pkg:gem/dry-monads#lib/dry/monads/task.rb:269
    def [](executor); end
  end
end

# Task constructors
#
# pkg:gem/dry-monads#lib/dry/monads/task.rb:284
module Dry::Monads::Task::Mixin::Constructors
  # Builds a new Task instance.
  #
  # @param block [Proc]
  # @return Task
  #
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:289
  def Task(&_arg0); end
end

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/task.rb:260
Dry::Monads::Task::Mixin::Task = Dry::Monads::Task

# @see Dry::Monads::Unit
#
# pkg:gem/dry-monads#lib/dry/monads/task.rb:263
Dry::Monads::Task::Mixin::Unit = T.let(T.unsafe(nil), Object)

# @api private
#
# pkg:gem/dry-monads#lib/dry/monads/task.rb:14
class Dry::Monads::Task::Promise < ::Concurrent::Promise
  # pkg:gem/dry-monads#lib/dry/monads/task.rb:15
  def on_fulfill(result); end

  # pkg:gem/dry-monads#lib/dry/monads/task.rb:15
  def on_reject(reason); end
end

# Advanced transformations.
#
# pkg:gem/dry-monads#lib/dry/monads/transformer.rb:6
module Dry::Monads::Transformer
  # Lifts a block/proc over the 2-level nested structure.
  # This is essentially fmap . fmap (. is the function composition
  # operator from Haskell) or the functor instance for
  # a two-level monadic structure like List Either.
  #
  # @example
  #   List[Right(1), Left(1)].fmap2 { |x| x + 1 } # => List[Right(2), Left(1)]
  #   Right(None).fmap2 { |x| x + 1 } # => Right(None)
  #
  # @param args [Array<Object>] arguments will be passed to the block or the proc
  # @return [Object] some monadic value
  #
  # pkg:gem/dry-monads#lib/dry/monads/transformer.rb:18
  def fmap2(*args); end

  # Lifts a block/proc over the 3-level nested structure.
  #
  # @example
  #   List[Right(Some(1)), Left(Some(1))].fmap3 { |x| x + 1 }
  #   # => List[Right(Some(2)), Left(Some(1))]
  #   Right(None).fmap3 { |x| x + 1 } # => Right(None)
  #
  # @param args [Array<Object>] arguments will be passed to the block or the proc
  # @return [Object] some monadic value
  #
  # pkg:gem/dry-monads#lib/dry/monads/transformer.rb:36
  def fmap3(*args); end
end

# List of default traverse functions for types.
# It is implicitly used by List#traverse for
# making common cases easier to handle.
#
# pkg:gem/dry-monads#lib/dry/monads/traverse.rb:15
Dry::Monads::Traverse = T.let(T.unsafe(nil), Hash)

# Represents a value which can be either success or a failure (an exception).
# Use it to wrap code that can raise exceptions.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:9
class Dry::Monads::Try
  # Returns true for an instance of a {Try::Error} monad.
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:80
  def error?; end

  # @return [Exception] Caught exception
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:16
  def exception; end

  # pkg:gem/dry-monads#lib/dry/monads/try.rb:81
  def failure?; end

  # pkg:gem/dry-monads#lib/dry/monads/try.rb:77
  def success?; end

  # Returns self.
  #
  # @return [Try::Value, Try::Error]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:86
  def to_monad; end

  # Returns true for an instance of a {Try::Value} monad.
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:76
  def value?; end

  class << self
    # Safely runs a block
    #
    # @example using Try with [] and a block (Ruby 2.5+)
    #   include Dry::Monads::Try::Mixin
    #
    #   def safe_db_call
    #     Try[DatabaseError] { db_call }
    #   end
    #
    # @param exceptions [Array<Exception>]
    # @return [Try::Value,Try::Error]
    #
    # pkg:gem/dry-monads#lib/dry/monads/try.rb:68
    def [](*exceptions, &block); end

    # pkg:gem/dry-monads#lib/dry/monads/try.rb:33
    def lift(*args, &block); end

    # Wraps a value with Value
    #
    # @overload pure(value, exceptions = DEFAULT_EXCEPTIONS)
    #   @param value [Object] value for wrapping
    #   @param exceptions [Array<Exceptions>] list of exceptions to rescue
    #   @return [Try::Value]
    #
    # @overload pure(exceptions = DEFAULT_EXCEPTIONS, &block)
    #   @param exceptions [Array<Exceptions>] list of exceptions to rescue
    #   @param block [Proc] value for wrapping
    #   @return [Try::Value]
    #
    # pkg:gem/dry-monads#lib/dry/monads/try.rb:47
    def pure(value = T.unsafe(nil), exceptions = T.unsafe(nil), &block); end

    # Invokes a callable and if successful stores the result in the
    # {Try::Value} type, but if one of the specified exceptions was raised it stores
    # it in a {Try::Error}.
    #
    # @param exceptions [Array<Exception>] list of exceptions to rescue
    # @param f [#call] callable object
    # @return [Try::Value, Try::Error]
    #
    # pkg:gem/dry-monads#lib/dry/monads/try.rb:28
    def run(exceptions, f); end
  end
end

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:11
Dry::Monads::Try::DEFAULT_EXCEPTIONS = T.let(T.unsafe(nil), Array)

# Represents a result of a failed execution.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:171
class Dry::Monads::Try::Error < ::Dry::Monads::Try
  include ::Dry::Core::Equalizer::Methods
  include ::Dry::Monads::RightBiased::Left

  # @param exception [Exception]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:178
  def initialize(exception); end

  # @param other [Try]
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:208
  def ===(other); end

  # pkg:gem/dry-monads#lib/dry/monads/try.rb:186
  def inspect; end

  # If a block is given passes internal value to it and returns the result,
  # otherwise simply returns the first argument.
  #
  # @example
  #   Try(ZeroDivisionError) { 1 / 0 }.or { "zero!" } # => "zero!"
  #
  # @param args [Array<Object>] arguments that will be passed to a block
  #                             if one was given, otherwise the first
  #                             value will be returned
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:198
  def or(*args); end

  # Acts in a similar way to `rescue`. It checks if
  # {exception} is one of {errors} and yields the block if so.
  #
  # @param errors [Class] List of Exception subclasses
  #
  # @return [Try::Value]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:216
  def recover(*errors); end

  # @return [Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:395
  def to_maybe; end

  # @return [Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:414
  def to_result; end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:185
  def to_s; end

  class << self
    # pkg:gem/dry-monads#lib/dry/monads/try.rb:175
    def call(*_arg0); end
  end
end

# A module that can be included for easier access to Try monads.
#
# @example
#   class Foo
#     include Dry::Monads::Try::Mixin
#
#     attr_reader :average
#
#     def initialize(total, count)
#       @average = Try(ZeroDivisionError) { total / count }.value
#     end
#   end
#
#   Foo.new(10, 2).average # => 5
#   Foo.new(10, 0).average # => nil
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:246
module Dry::Monads::Try::Mixin
  include ::Dry::Monads::Try::Mixin::Constructors

  # Error constructor
  #
  # @overload Error(value)
  #   @param error [Exception]
  #   @return [Try::Error]
  #
  # @overload Error(&block)
  #   @param block [Proc] a block to be wrapped with Error
  #   @return [Try::Error]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:294
  def Error(error = T.unsafe(nil), &block); end

  # Value constructor
  #
  # @overload Value(value)
  #   @param value [Object]
  #   @return [Try::Value]
  #
  # @overload Value(&block)
  #   @param block [Proc] a block to be wrapped with Value
  #   @return [Try::Value]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:277
  def Value(value = T.unsafe(nil), exceptions = T.unsafe(nil), &block); end
end

# @private
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:251
module Dry::Monads::Try::Mixin::Constructors
  # A convenience wrapper for {Monads::Try.run}.
  # If no exceptions are provided it falls back to StandardError.
  # In general, relying on this behaviour is not recommended as it can lead to unnoticed
  # bugs and it is always better to explicitly specify a list of exceptions if possible.
  #
  # @param exceptions [Array<Exception>]
  # @return [Try]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:259
  def Try(*exceptions, &f); end
end

# @see Dry::Monads::Try
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:248
Dry::Monads::Try::Mixin::Try = Dry::Monads::Try

# Represents a result of a successful execution.
#
# @api public
#
# pkg:gem/dry-monads#lib/dry/monads/try.rb:91
class Dry::Monads::Try::Value < ::Dry::Monads::Try
  include ::Dry::Core::Equalizer::Methods
  include ::Dry::Monads::RightBiased::Right

  # @param exceptions [Array<Exception>] list of exceptions to be rescued
  # @param value [Object] the value to be stored in the monad
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:100
  def initialize(exceptions, value); end

  # Calls the passed in Proc object with value stored in self
  # and returns the result.
  #
  # If proc is nil, it expects a block to be given and will yield to it.
  #
  # @example
  #   success = Dry::Monads::Try::Value.new(ZeroDivisionError, 10)
  #   success.bind(->(n) { n / 2 }) # => 5
  #   success.bind { |n| n / 0 } # => Try::Error(ZeroDivisionError: divided by 0)
  #
  # @param args [Array<Object>] arguments that will be passed to a block
  #                             if one was given, otherwise the first
  #                             value assumed to be a Proc (callable)
  #                             object and the rest of args will be passed
  #                             to this object along with the internal value
  # @return [Object, Try::Error]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:126
  def bind(*_arg0, **_arg1, &_arg2); end

  # @return [Array<Exception>] List of exceptions to rescue
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:96
  def catchable; end

  # Does the same thing as #bind except it also wraps the value
  # in an instance of a Try monad. This allows for easier
  # chaining of calls.
  #
  # @example
  #   success = Dry::Monads::Try::Value.new(ZeroDivisionError, 10)
  #   success.fmap(&:succ).fmap(&:succ).value # => 12
  #   success.fmap(&:succ).fmap { |n| n / 0 }.fmap(&:succ).value # => nil
  #
  # @param args [Array<Object>] extra arguments for the block, arguments are being processes
  #                             just as in #bind
  # @return [Try::Value, Try::Error]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:144
  def fmap(*_arg0, **_arg1, &_arg2); end

  # pkg:gem/dry-monads#lib/dry/monads/try.rb:158
  def inspect; end

  # Ignores values and returns self, see {Try::Error#recover}
  #
  # @param _errors [Class] List of Exception subclasses
  #
  # @return [Try::Value]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:165
  def recover(*_errors, &_arg1); end

  # @return [Maybe]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:390
  def to_maybe; end

  # @return [Result::Success]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:409
  def to_result; end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/try.rb:151
  def to_s; end

  private

  # pkg:gem/dry-monads#lib/dry/monads/try.rb:107
  def bind_call(*args, **kwargs); end

  class << self
    # pkg:gem/dry-monads#lib/dry/monads/try.rb:93
    def call(*_arg0); end

    # pkg:gem/dry-monads#lib/dry/monads/try.rb:93
    def to_proc; end
  end
end

# Unit is a special object you can use whenever your computations don't
# return any payload. Previously, if your function ran a side-effect
# and returned no meaningful value, you had to return things like
# Success(nil), Success([]), Success({}), Maybe(""), Success(true) and
# so forth.
#
# You should use Unit if you wish to return an empty monad.
#
# @example with Result
#   Success(Unit)
#   Failure(Unit)
#
# @example with Maybe
#   Maybe(Unit)
#   Some(Unit)
#
# pkg:gem/dry-monads#lib/dry/monads/unit.rb:21
Dry::Monads::Unit = T.let(T.unsafe(nil), Object)

# An unsuccessful result of extracting a value from a monad.
#
# pkg:gem/dry-monads#lib/dry/monads/errors.rb:6
class Dry::Monads::UnwrapError < ::StandardError
  # pkg:gem/dry-monads#lib/dry/monads/errors.rb:9
  def initialize(receiver); end

  # pkg:gem/dry-monads#lib/dry/monads/errors.rb:7
  def receiver; end
end

# @see Validated::Valid
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:271
Dry::Monads::Valid = Dry::Monads::Validated::Valid

# Validated is similar to Result and represents an outcome of a validation.
# The difference between Validated and Result is that the former implements
# `#apply` in a way that concatenates errors. This means that the error type
# has to have `+` implemented (be a semigroup). This plays nice with arrays and lists.
# Also, List<Validated>#traverse implicitly uses a block that wraps errors with
# a list so that you don't have to do it manually.
#
# @example using with List
#   List::Validated[Valid('London'), Invalid(:name_missing), Invalid(:email_missing)]
#   # => Invalid(List[:name_missing, :email_missing])
#
# @example with valid results
#   List::Validated[Valid('London'), Valid('John')]
#   # => Valid(List['London', 'John'])
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:20
class Dry::Monads::Validated
  # Bind/flat_map is not implemented
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:46
  def bind(*_arg0, **_arg1, &_arg2); end

  # Returns self.
  #
  # @return [Validated::Valid, Validated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:42
  def to_monad; end

  class << self
    # Wraps a value with `Valid`.
    #
    # @overload pure(value)
    #   @param value [Object] value to be wrapped with Valid
    #   @return [Validated::Valid]
    #
    # @overload pure(&block)
    #   @param block [Object] block to be wrapped with Valid
    #   @return [Validated::Valid]
    #
    # pkg:gem/dry-monads#lib/dry/monads/validated.rb:34
    def pure(value = T.unsafe(nil), &block); end
  end
end

# Invalid result
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:132
class Dry::Monads::Validated::Invalid < ::Dry::Monads::Validated
  include ::Dry::Core::Equalizer::Methods

  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:146
  def initialize(error, trace = T.unsafe(nil)); end

  # @param other [Object]
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:210
  def ===(other); end

  # Lifts a block/proc over Invalid
  #
  # @overload alt_map(proc)
  #   @param proc [#call]
  #   @return [Validated::Invalid]
  #
  # @overload alt_map
  #   @param block [Proc]
  #   @return [Validated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:180
  def alt_map(proc = T.unsafe(nil), &block); end

  # Collects errors (ignores valid results)
  #
  # @overload apply(val)
  #   @param val [Validated::Valid,Validated::Invalid]
  #   @return [Validated::Invalid]
  #
  # @overload apply
  #   @yieldreturn [Validated::Valid,Validated::Invalid]
  #   @return [Validated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:163
  def apply(val = T.unsafe(nil), &block); end

  # The value stored inside
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:136
  def error; end

  # Ignores the passed argument and returns self
  #
  # @return [Validated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:188
  def fmap(_ = T.unsafe(nil), &_arg1); end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:205
  def inspect; end

  # Yields the given callable and returns the result
  #
  # @overload or(proc)
  #   @param proc [#call]
  #   @return [Object]
  #
  # @overload or
  #   @param block [Proc]
  #   @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:200
  def or(proc = T.unsafe(nil), &block); end

  # Converts to Maybe::None
  #
  # @return [Maybe::None]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:411
  def to_maybe; end

  # Converts to Result::Failure
  #
  # @return [Result::Failure]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:432
  def to_result; end

  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:206
  def to_s; end

  # Line where the value was constructed
  #
  # @return [String]
  # @api public
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:142
  def trace; end
end

# Mixin with Validated constructors
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:217
module Dry::Monads::Validated::Mixin
  include ::Dry::Monads::Validated::Mixin::Constructors
end

# Actual constructor methods
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:228
module Dry::Monads::Validated::Mixin::Constructors
  # Invalid constructor
  #
  # @overload Invalid(value)
  #   @param value [Object]
  #   @return [Valdated::Invalid]
  #
  # @overload Invalid(&block)
  #   @param block [Proc]
  #   @return [Valdated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:256
  def Invalid(value = T.unsafe(nil), &block); end

  # Valid constructor
  #
  # @overload Valid(value)
  #   @param value [Object]
  #   @return [Valdated::Valid]
  #
  # @overload Valid(&block)
  #   @param block [Proc]
  #   @return [Valdated::Valid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:239
  def Valid(value = T.unsafe(nil), &block); end
end

# Unsuccessful validation result
# @see Dry::Monads::Validated::Invalid
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:224
Dry::Monads::Validated::Mixin::Invalid = Dry::Monads::Validated::Invalid

# Successful validation result
# @see Dry::Monads::Validated::Valid
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:220
Dry::Monads::Validated::Mixin::Valid = Dry::Monads::Validated::Valid

# Valid result
#
# pkg:gem/dry-monads#lib/dry/monads/validated.rb:54
class Dry::Monads::Validated::Valid < ::Dry::Monads::Validated
  include ::Dry::Core::Equalizer::Methods

  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:57
  def initialize(value); end

  # @param other [Object]
  # @return [Boolean]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:125
  def ===(other); end

  # Ignores values and returns self, see {Invalid#alt_map}
  #
  # @return [Validated::Valid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:106
  def alt_map(_ = T.unsafe(nil), &_arg1); end

  # Applies another Valid to the stored function
  #
  # @overload apply(val)
  #   @example
  #     Validated.pure { |x| x + 1 }.apply(Valid(2)) # => Valid(3)
  #
  #   @param val [Validated::Valid,Validated::Invalid]
  #   @return [Validated::Valid,Validated::Invalid]
  #
  # @overload apply
  #   @example
  #     Validated.pure { |x| x + 1 }.apply { Valid(4) } # => Valid(5)
  #
  #   @yieldreturn [Validated::Valid,Validated::Invalid]
  #   @return [Validated::Valid,Validated::Invalid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:84
  def apply(val = T.unsafe(nil), &_arg1); end

  # Lifts a block/proc over Valid
  #
  # @overload fmap(proc)
  #   @param proc [#call]
  #   @return [Validated::Valid]
  #
  # @overload fmap
  #   @param block [Proc]
  #   @return [Validated::Valid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:98
  def fmap(proc = T.unsafe(nil), &block); end

  # @return [String]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:114
  def inspect; end

  # Ignores arguments, returns self
  #
  # @return [Validated::Valid]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:111
  def or(_ = T.unsafe(nil), &_arg1); end

  # Converts to Maybe::Some
  #
  # @return [Maybe::Some]
  #
  # pkg:gem/dry-monads#lib/dry/monads/maybe.rb:404
  def to_maybe; end

  # Converts to Result::Success
  #
  # @return [Result::Success]
  #
  # pkg:gem/dry-monads#lib/dry/monads/result.rb:425
  def to_result; end

  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:121
  def to_s; end

  # Extracts the value
  #
  # @return [Object]
  #
  # pkg:gem/dry-monads#lib/dry/monads/validated.rb:66
  def value!; end
end
